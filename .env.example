# NAS 上希望 Codex 操作的目录。相对路径以 compose.yaml 所在目录为基准。
CODEX_WORKSPACE=./workspace

# 控制台默认仅 NAS 本机可访问。需要从局域网访问时改为 NAS 的局域网 IP，
# 例如 192.168.1.20。不要直接暴露到公网。
DASHBOARD_BIND_IP=127.0.0.1
DASHBOARD_PORT=8765

# 与本项目验证过的 Codex CLI 版本；升级前建议重新运行测试。
CODEX_VERSION=0.145.0
LIVIS_CODEX_IMAGE=livis-codex:local
